# 🛒 Система Электронной Коммерции (MVC)

![Project Status](https://img.shields.io/badge/Status-Completed-success)
![License](https://img.shields.io/badge/License-MIT-blue)
![Tech Stack](https://img.shields.io/badge/Node.js-Express-green)

Профессиональная реализация интернет-магазина, разработанная для демонстрации архитектурного паттерна **MVC (Model-View-Controller)** и классических шаблонов проектирования **GoF**.

Проект сочетает в себе строгую серверную архитектуру на **Node.js** и современный, минималистичный пользовательский интерфейс.

---

## ✨ Ключевые Возможности

### � Для Покупателя
*   **Каталог товаров**: Просмотр списка товаров с динамической сортировкой (по цене, по имени).
*   **Корзина**: Добавление нескольких товаров, просмотр состава заказа и расчет итоговой суммы.
*   **Оформление заказа**: Симуляция процесса покупки с проверкой остатков на складе и списанием средств.

### 👨‍💻 Для Администратора
*   **Управление пользователями**: Полный цикл CRUD (Создание, Чтение, Обновление, Удаление).
*   **Аналитика**: Генерация отчетов о продажах в фоновом режиме (без блокировки интерфейса).

---

## 🏗 Технологический Стек

| Компонент | Технология | Описание |
|-----------|------------|----------|
| **Core** | Node.js | Среда выполнения JavaScript |
| **Framework** | Express.js | Веб-сервер и маршрутизация |
| **Database** | MongoDB | NoSQL база данных для хранения товаров и пользователей |
| **ODM** | Mongoose | Моделирование данных и валидация |
| **UI** | EJS + CSS3 | Шаблонизатор и стили (Glassmorphism) |

---

## 🏛 Архитектура и Паттерны
В проекте реализованы 7 ключевых паттернов проектирования:

1.  **Singleton**: Единая точка подключения к базе данных.
2.  **Factory Method**: Централизованное создание пользователей разных ролей.
3.  **Facade**: Упрощение сложной логики покупки (проверка → оплата → склад).
4.  **Adapter**: Интеграция современной системы с Legacy-платежным шлюзом.
5.  **Observer**: Логирование важных действий пользователей.
6.  **Strategy**: Гибкая смена алгоритмов сортировки товаров.
7.  **Producer-Consumer**: Вынос тяжелых задач (генерация отчетов) в Worker Threads.

---

## 🚀 Установка и Запуск

Мы подготовили подробную инструкцию для всех популярных ОС.

### 🪟 Windows (PowerShell)

**1. Установка окружения (Node.js + MongoDB)**
Откройте PowerShell и выполните команды (потребуется `winget`):
```powershell
winget install OpenJS.NodeJS.LTS
winget install MongoDB.Server
```
*После установки перезапустите терминал.*

**2. Запуск Базы Данных**
В отдельном окне терминала создайте папку и запустите Mongo:
```powershell
mkdir C:\data\db -Force
# Путь может отличаться в зависимости от версии (ниже для 8.0)
& "C:\Program Files\MongoDB\Server\8.0\bin\mongod.exe" --dbpath="C:\data\db"
```

**3. Запуск Проекта**
В основном окне терминала:
```powershell
# Перейдите в папку проекта
npm install
node app.js
```

---

### 🍎 macOS

**1. Установка окружения**
Используйте Homebrew:
```bash
# Если нет Homebrew
/bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

# Установка компонентов
brew install node
brew tap mongodb/brew
brew install mongodb-community

# Запуск сервиса БД
brew services start mongodb-community
```

**2. Запуск Проекта**
```bash
npm install
node app.js
```

---

### 🐧 Linux (Ubuntu/Debian)

**1. Установка**
```bash
sudo apt update
sudo apt install -y nodejs npm

# Установка MongoDB (пример)
sudo apt install -y mongodb
sudo systemctl start mongodb
sudo systemctl enable mongodb
```

**2. Запуск Проекта**
```bash
npm install
node app.js
```

---

## ✅ Проверка работоспособности

После запуска сервера (`node app.js`):

1.  Откройте браузер: [http://localhost:3000/products](http://localhost:3000/products)
2.  Вы увидите каталог товаров.
3.  Перейдите в `/admin/report`, чтобы проверить генерацию отчетов.




